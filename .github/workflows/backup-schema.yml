name: Backup Supabase Schema

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/backup-schema.yml'

jobs:
  backup:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
          
      - name: Backup Database Schema
        env:
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
        run: |
          mkdir -p database/backups
          
          # Use pooler connection on port 6543 (supports both IPv4 and IPv6)
          DB_HOST="aws-0-eu-central-1.pooler.supabase.com"
          
          echo "Connecting to Supabase pooler..."
          PGPASSWORD=$SUPABASE_DB_PASSWORD pg_dump \
            -h "$DB_HOST" \
            -p 6543 \
            -U "postgres.${SUPABASE_PROJECT_ID}" \
            -d postgres \
            --schema-only \
            --schema=public \
            --no-owner \
            --no-privileges \
            -f database/backups/schema_backup_$(date +%Y%m%d).sql
          
          echo "✅ Backup completed!"
          ls -lh database/backups/schema_backup_$(date +%Y%m%d).sql
          
          # Keep only last 7 days of backups
          cd database/backups
          ls -t schema_backup_*.sql | tail -n +8 | xargs -r rm || true
          
      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add database/backups/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🔄 Automatic schema backup $(date +%Y-%m-%d)"
            git push
          fi
